// import { Route } from "react-router-dom";

// function PrivateRoute({ children, ...rest }) {
//   let isAuthenticated = loacalStorage.getItem("token") ? true : false;
//   return (
//     <Route
//       {...rest}
//       render={() =>
//         isAuthenticated ? children : <Redirect to="/login" replace />
//       }
//     />
//   );
// }

// export default PrivateRoute;
